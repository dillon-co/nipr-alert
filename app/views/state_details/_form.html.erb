<%= form_for(state_detail) do |f| %>
  <% if state_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(state_detail.errors.count, "error") %> prohibited this state_detail from being saved:</h2>

      <ul>
      <% state_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
